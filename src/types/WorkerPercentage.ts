/**
 * @property {string} roleId - The ID of the role.
 * @property {float} percentage - The percentage value (Must be a float number).
 */

export type WorkerPercentage = {
    roleId: string;
    percentage: number;
}